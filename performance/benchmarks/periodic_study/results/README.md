# Compact recorded evidence

The CSV tables, reference qualifications, environment, LAPACK baseline and plots
support the [historical report](../REPORT.md). The original final source is kept
as a compressed archive. Large raw logs, spectrum arrays and duplicate JSON
exports are omitted. Historical method names are not current public APIs.

`SHA256SUMS` records hashes for this compact snapshot. Original provenance records
refer to the complete earlier artifact; see the parent README for recovery context.
